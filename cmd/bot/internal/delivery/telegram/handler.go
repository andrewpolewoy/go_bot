package telegramdelivery

type TelegramHandler struct {
	// service *service.NotifierService
}

func NewTelegramHandler( /* deps */ ) *TelegramHandler {
	return &TelegramHandler{}
}

// сюда придёт логика обработки /start, /setgithub <login> и т.п.
